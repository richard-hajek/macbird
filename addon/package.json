{
  "name": "thunderbird-mcp-addon",
  "version": "1.0.0",
  "description": "Thunderbird WebExtension for MCP Server",
  "scripts": {
    "lint": "eslint *.js",
    "lint:fix": "eslint *.js --fix",
    "build": "mkdir -p dist && rm dist/package.zip && atool -a dist/package.zip *.js *.json _locales/**/*"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
